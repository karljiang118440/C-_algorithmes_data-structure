 #if 0       
        unordered_map<int,int> umap;
        for(int i=0;i<n;i++)
        {
            umap[A[i]]++;
        }
        for (int i=0;i<n;i++)
        {
            if(umap[A[i]]==1)
                return A[i];
        }
        
    }
 #endif